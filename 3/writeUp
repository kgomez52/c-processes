The first function we will look at is getpid(). This function is called by a process. The function will return the process ID of the calling process. The next function to look at is fprintf(). This function formats and writes a series of characters and values to the ouput stream. The function returns the number of bytes that are printed or a negative value if an output error occurs. 
